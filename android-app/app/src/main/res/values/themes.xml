<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         SmartRoad LIGHT theme (Material Design 3).
         All screens use ?attr/... so colors are never hard-coded.
         Dark theme overrides live in values-night/themes.xml.
         ============================================================ -->
    <style name="Theme.SmartRoad" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/onPrimaryColor</item>
        <item name="colorPrimaryContainer">@color/primaryColor</item>
        <item name="colorOnPrimaryContainer">@color/onPrimaryColor</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/onSecondaryColor</item>
        <item name="colorSecondaryContainer">@color/secondaryColor</item>
        <item name="colorOnSecondaryContainer">@color/onSecondaryColor</item>

        <!-- Accent mapped to tertiary -->
        <item name="colorTertiary">@color/accentColor</item>
        <item name="colorOnTertiary">@color/onPrimaryColor</item>

        <!-- Surfaces / background -->
        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="colorSurface">@color/surfaceColor</item>
        <item name="colorOnSurface">@color/textPrimaryColor</item>
        <item name="colorOnSurfaceVariant">@color/textSecondaryColor</item>

        <!-- Error -->
        <item name="colorError">@color/errorColor</item>
        <item name="colorOnError">@color/onPrimaryColor</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="android:navigationBarColor">@color/surfaceColor</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- ===== Reusable component styles (all theme-driven) ===== -->

    <style name="Widget.SmartRoad.Button" parent="Widget.Material3.Button">
        <item name="android:minHeight">@dimen/button_height</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">@dimen/card_radius</item>
    </style>

    <style name="Widget.SmartRoad.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:minHeight">@dimen/button_height</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">@dimen/card_radius</item>
    </style>

    <style name="Widget.SmartRoad.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">@dimen/card_radius</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="contentPadding">@dimen/space_md</item>
    </style>

    <style name="Widget.SmartRoad.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">@dimen/card_radius</item>
        <item name="boxCornerRadiusTopEnd">@dimen/card_radius</item>
        <item name="boxCornerRadiusBottomStart">@dimen/card_radius</item>
        <item name="boxCornerRadiusBottomEnd">@dimen/card_radius</item>
    </style>

    <style name="Text.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Text.Body" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <!-- Hazard Map: status/category filter chips (checkable, single-selection groups) -->
    <style name="Widget.SmartRoad.Chip.Filter" parent="Widget.Material3.Chip.Filter">
        <item name="android:textSize">13sp</item>
        <item name="chipMinHeight">34dp</item>
    </style>

    <!-- Hazard Map: compact, non-interactive legend entries -->
    <style name="Widget.SmartRoad.Chip.Legend" parent="Widget.Material3.Chip.Assist">
        <item name="android:clickable">false</item>
        <item name="android:focusable">false</item>
        <item name="android:textSize">11sp</item>
        <item name="chipMinHeight">28dp</item>
        <item name="chipIconSize">14dp</item>
        <item name="chipStartPadding">6dp</item>
        <item name="chipEndPadding">8dp</item>
    </style>
</resources>
